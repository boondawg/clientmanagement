<%= form_for(@note_template) do |f| %>
  <% if @note_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note_template.errors.count, "error") %> prohibited this note_template from being saved:</h2>

      <ul>
      <% @note_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
